<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        style="@style/header_style">
        <TextView
            style="@style/header_text"
            android:text = "@string/menu">
        </TextView>

        <Button
            android:id="@+id/from_menu_to_register"
            style="@style/header_button"
            android:text="@string/reg"></Button>
    </RelativeLayout>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/scroll_view_menu">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="horizontal">
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_weight=".16"/>
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_weight=".1">

                <Button
                    android:id="@+id/from_menu_to_dishes"
                    style="@style/menu_button"
                    android:text="@string/call"></Button>

                <Button
                    android:id="@+id/from_menu_to_exercises"
                    style="@style/menu_button"
                    android:text="@string/exercises"></Button>

                <Button
                    android:id="@+id/from_menu_to_water"
                    style="@style/menu_button"
                    android:text="@string/water"></Button>

                <Button
                    android:id="@+id/from_menu_to_statistic"
                    style="@style/menu_button"
                    android:text="@string/sta"></Button>

                <Button
                    android:id="@+id/exit"
                    style="@style/menu_button"
                    android:onClick="onExitButton"
                    android:text="@string/exit"></Button>
            </LinearLayout>
            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_weight=".16"/>
        </LinearLayout>
    </ScrollView>
</LinearLayout>
